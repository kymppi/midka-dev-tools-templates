name = "Blank"
description = "A template for blank projects"
language = ["Rust", "TypeScript", "JavaScript"]

[init_commands]
rust = "cargo init . --bin"

[install_commands]
typescript = "npm install"
javascript = "npm install"